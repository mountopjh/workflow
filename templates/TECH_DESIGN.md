# 技术文档（TECH_DESIGN）

> 描述"用什么方案、按什么架构落地"。是 PRD 与执行方案之间的桥梁。  
> 写到接口签名 + 数据结构层级即可，**不写完整实现代码**。

---

## 元信息

| 字段 | 值 |
|---|---|
| 版本 | V1.0 |
| 生成时间 | YYYY-MM-DD HH:MM:SS |
| 关联 PRD 版本 | PRD V1.0 |

---

## 1. 总体架构

### 1.1 架构决策与权衡
- 选定方案：<一句话>
- 主要替代方案：<一句话> + 排除理由（对照 PRD §2 哪条硬约束）
- 重要权衡：<可读性 / 可维护性 / 性能 等的取舍说明>

### 1.2 模块划分图
```
[模块 A] ──> [模块 B] ──> [模块 C]
      ↘
       [模块 D]
```

### 1.3 关键数据流
1. 输入 → 模块 A 处理 → ...
2. ...

---

## 2. 模块详述

### 2.1 模块 A · <名称>
- 职责：<一句话>
- 关键接口：
  - `function_name(arg1: Type, arg2: Type) -> Return`：<功能说明>
- 关键数据结构：
  ```
  Entity {
    field_a: Type   # 含义
    field_b: Type
  }
  ```
- 依赖：模块 X / 第三方库 Y

### 2.2 模块 B · <名称>
...

---

## 3. 接口契约

### 3.1 内部接口
- 模块 A ↔ 模块 B：<协议 / 数据格式 / 错误码>

### 3.2 外部接口（如有）
- HTTP API / CLI / SDK 等的对外契约

---

## 4. 数据存储

### 4.1 表 / 集合 / 文件结构
- `table_name`：字段、索引、约束

### 4.2 数据迁移策略
- <如有>

---

## 5. 横切关注

### 5.1 错误处理策略
- 错误分级：<致命 / 可恢复 / 警告>
- 各级处理方式：<抛出 / 重试 / 日志 + 降级>

### 5.2 日志与可观测性
- 关键日志埋点：<位置 + 内容>
- 错误日志上下文要求：<必带字段>

### 5.3 安全
- 鉴权 / 授权 / 输入校验 等关键点

### 5.4 性能基线
- 与 PRD §4.2 对应的实现层目标
- 关键路径的复杂度

---

## 6. 不在本文件内的内容
- 任务序列 → 见 EXECUTION_PLAN.md
- 业务目标 → 见 PRD.md
- 完整代码 → 不写，由执行者按工单实现
